node {

    stage('Checkout') {
        checkout scm
    }

    stage('Build') {
        echo 'Deploy da Vinheria'
    }

    stage('Deploy') {
        sh '''
        mkdir -p /tmp/vinheria
        cp -r $WORKSPACE/* /tmp/vinheria/
        '''
    }
}